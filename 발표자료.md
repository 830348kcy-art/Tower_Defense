# Team-Tower_Defense

## WPF 기반 타워 디펜스 게임

> Kingdom Rush 스타일 · 20 스테이지 · 5종 타워 · 12종 적
> .NET 8 / WPF / C# 12
> Author: 김철영, 김도건, 최형선

---

## 1. 프로젝트 개요

- **장르** : 2D 타워 디펜스
- **플랫폼** : Windows Desktop (.NET 8 / WPF)
- **언어** : C# 12 + XAML
- **렌더링** : `Canvas` + 프로그래매틱 도형 — **이미지 에셋 0개**, 모든 비주얼이 코드
- **저장** : `%LOCALAPPDATA%\KingdomRushClone\save.json` (JSON 직렬화)
- **테스트** : 별도 `Tests` 프로젝트 (4 단위 테스트, 전원 PASS)
- **개발 기간** : 약 4주 · 약 7,000 LoC

### 왜 Kingdom Rush인가?

> Kingdom Rush는 타워 배치·업그레이드·스킬 사용의 3박자가 완벽한 장르 교과서.
> WPF Canvas 렌더링과 OOP 설계 양쪽을 동시에 학습할 수 있는 최적의 주제였음.

---

## 2. 게임 핵심 루프 (Core Loop)

```
스테이지 선택
    ↓  인트로 (출현 적 미리보기)
타일 클릭 → 타워 건설 (5종 선택)
    ↓
웨이브 진행 → 적 처치 → 골드 획득
    ↓
타워 업그레이드 (Lv1→2→3) / Lv3 분기 선택 (A or B)
    ↓
N 키로 다음 웨이브 조기 호출 (잔여시간 × 2 골드 보너스)
    ↓
승리 → 별 1~3개 → 테크트리 영구 강화 → 다음 스테이지 해금
```

- **피드백 루프 설계**
  - 짧은 웨이브(~20s) → 즉각적인 성취감
  - 조기 호출 보너스 → 공격적 플레이 유도
  - 별 수집 → 테크트리 → 어려운 스테이지 도전 → 반복

---

## 3. 기술 스택 & 아키텍처

```
KingdomRushClone/
├─ Models/      ← 순수 데이터 (Vec2, TowerDef, EnemyDef, WaveDef…)
├─ Data/        ← 카탈로그 (TowerCatalog, EnemyCatalog, StageCatalog)
├─ Game/        ← 런타임 로직 (GameEngine, WaveSpawner, TowerInstance, Projectile…)
├─ Views/       ← WPF 페이지 + Factory (GamePage, EnemyFallbackImageFactory…)
├─ Managers/    ← 영속성 (SaveManager — JSON 직렬화)
└─ Tests/       ← 단위 테스트 프로젝트
```

### 핵심 설계 원칙

| 원칙                  | 적용 방식                                                         |
| ------------------- | ------------------------------------------------------------- |
| **SSOT** (단일 진실 출처) | TowerCatalog 한 줄 수정 → 게임·도감·업그레이드 메뉴 자동 반영                    |
| **MVVM-라이트**        | GameEngine이 `StateChanged` 이벤트 발생 → GamePage가 구독해 UI 갱신       |
| **불변 데이터**          | `TowerDef`, `EnemyDef` — readonly 카탈로그, 런타임에는 Instance 객체만 변경 |
| **팩토리 패턴**          | `EnemyFallbackImageFactory` — 12종 스프라이트를 코드로 생성·캐시            |

---

## 4. 타워 시스템 — 5종 소개

| 아이콘 | 이름     | 역할                | 분기 A               | 분기 B               |
|:---:| ------ | ----------------- | ------------------ | ------------------ |
| 🏹  | 아처 타워  | 단일 원거리 · 치명타 12%  | 🎯 정밀 사수 (크리 25%)  | ⚡ 폭풍 사수 (3중 연사)    |
| 🔮  | 마법사 타워 | 마법 피해 + 슬로우       | ❄ 서리 마법사 (광역 빙결)   | 🔥 화염 마법사 (광역+도트)  |
| 💣  | 폭격 타워  | 탄도 광역 폭발 (포물선 궤적) | 🎆 박격포 (초장거리)      | 💥 지뢰 포대 (즉발 대폭발)  |
| ⚔   | 병영     | 근접 병사 소환 · 경로 차단  | 🛡 성기사 부대 (고체력 탱커) | 🗡 도적 부대 (고DPS 딜러) |
| ❄   | 슬로우 타워 | 광역 둔화 서포트         | —                  | —                  |

- **레벨 분기**: Lv1→2→3 직선 업그레이드, **Lv3에서 A 또는 B 분기 선택 (변경 불가)**
- **판매**: 투자 골드의 **70% 환원**

---

## 5. 타워 메커니즘 심화

### 타겟팅 모드 (5종, 타워별 설정 가능)

```csharp
public enum TargetMode { First, Last, Strongest, Weakest, Flying }
```

| 모드        | 설명                   | 추천 타워       |
| --------- | -------------------- | ----------- |
| First     | 경로 진행률 가장 높은 적 우선    | 아처, 마법사     |
| Last      | 후미 적 우선 — 도망치는 약적 처리 | 슬로우         |
| Strongest | 최고체력 우선 — 보스 집중      | 폭격, 병영      |
| Weakest   | 최저체력 우선 — 잔적 정리      | 마법사 BranchB |
| Flying    | 비행 적 우선 — 와이번 카운터    | 아처, 마법사     |

### 크리티컬 시스템

```
Archer   기본 : 12% 확률, 2.2× 데미지 (흰색 → 노란색 텍스트)
BranchA  정밀사수 : 25% 확률, 동일 배율
BranchB  폭풍사수 : 3발 연사, 크리 없음
```

### 탄도 vs 호밍

- **폭격 타워** — `GetRenderPos()`: `arc = sin(π×t) × arcHeight` 포물선 궤적
- **그 외** — 매 프레임 적 위치 추적 (호밍 미사일)

---

## 6. 적 시스템 — 12종

### 기본 적 (8종)

| 아이콘 | 이름       | 특성                | 위협 포인트      |
|:---:| -------- | ----------------- | ----------- |
| 👺  | 고블린 병사   | 표준, 저렴            | 대량 스폰       |
| 🏃  | 고블린 스카우트 | **슬로우 면역**, 빠름    | 슬로우 무력화     |
| 🪖  | 오크 전사    | 물리 저항 35%, 고체력    | 아처·폭격에 강함   |
| 🐉  | 와이번      | **비행**, 마법 저항 20% | 병영 무효화      |
| 🧙  | 트롤 주술사   | **주변 적 HP 회복**    | 힐러 제거 우선 필요 |
| ⚔️  | 암흑기사     | 마법 저항 50%, 고방어    | 물리 타워 필수    |
| 👹  | 중간보스     | 고체력, 스테이지 5 등장    | —           |
| 💀  | 최종보스     | 최고 스탯, 스테이지 20    | —           |

### 공통 메커니즘

- **장갑 타입** : Light / Heavy / Magical / Flying — 타워별 피해 감소/증가 계산
- **상태 효과** : 슬로우(이동속도 감소), 도트(DoT 피해), 회복(TrollShaman)
- **DeathSpawns** : 사망 시 자식 적 생성 (분열 보스 체인 핵심)

---

## 7. 분열 보스 체인 (Split Boss Chain) ★

> 가장 독창적인 콘텐츠 — 단일 적이 사망 시 더 많은 적으로 분열

```
🟢 SplitBoss (HP 4,000)
        ↓ 사망
🟡 SplitMidBoss × 2 (HP 1,500 each)
        ↓ 각각 사망
🟢 SplitBody × 2×2 = 4 (HP 110 each)
        ↓ 각각 사망
🟢 SplitSmall × 3×4 = 12 (HP 35 each)
```

**최대 분열 시 생성 수**: 1 → 2 → 4 → 12 = **19 유닛**

### 구현 방식

```csharp
// EnemyDef에 DeathSpawns 리스트 추가
SplitBoss    → DeathSpawns = { SplitMidBoss, SplitMidBoss }
SplitMidBoss → DeathSpawns = { SplitBody,    SplitBody    }
SplitBody    → DeathSpawns = { SplitSmall,   SplitSmall,  SplitSmall }

// GameEngine.SpawnDeathSpawns() — 사망 처리 시 자동 호출
// 자식은 부모의 위치와 WaypointIndex를 그대로 상속 → 경로 이어받기
```

**전략적 의미**: SplitBoss를 너무 일찍 처치하면 잔 병력이 쏟아짐. 타이밍과 타워 배치 조절 필요.

---

## 8. 스테이지 구성 — 20 스테이지

| 범위    | 테마           | 경로 형태  | 특수 효과    | 보스                          |
| ----- | ------------ | ------ | -------- | --------------------------- |
| 1~4   | 🌿 Grassland | 직선     | —        | —                           |
| 5~8   | 🌲 Forest    | S커브    | —        | **5: 중간보스**                 |
| 9~12  | 🏜 Desert    | 양갈래 합류 | 좁은 경로    | **10: 보스**                  |
| 13~16 | 🌋 Volcano   | 지그재그   | 용암·야간 시야 | **13: 분열 중간보스 / 15: 분열 보스** |
| 17~20 | 🏰 Castle    | 3갈래 합류 | —        | **20: 최종 보스**               |

- **스테이지 해금**: 이전 스테이지 클리어 필수
- **허용 타워**: 스테이지 진행에 따라 순차 해금
  - Stage 1: 아처 + 슬로우 / Stage 2+: 병영 / Stage 4+: 마법사 / Stage 6+: 폭격

---

## 9. 웨이브 정책 & 경로 다양성

### 웨이브 수 정책

```
일반 스테이지         : 8 웨이브 (적 15마리 × 8파)
중간보스·보스 스테이지 : 10 웨이브 (9 일반 + 1 보스 웨이브)
```

### 보스 웨이브 구성 (코드)

```csharp
// 보스 웨이브 — OrcWarrior 10기 선봉 + Boss 1기 (지연 6초 등장)
bw.Entries.Add(new WaveEntry { Enemy = EnemyKind.OrcWarrior, Count = 10, SpawnInterval = 0.55 });
bw.Entries.Add(new WaveEntry { Enemy = BossFor(stage), Count = 1, InitialDelay = 6 });
```

### 경로 5종

| 형태     | 스테이지  | 특징                   |
| ------ | ----- | -------------------- |
| 직선     | 1~4   | 단순, 최단거리             |
| S커브    | 5~8   | 체류 시간 증가, 더 많은 배치 공간 |
| 양갈래 합류 | 9~12  | 두 방향 동시 방어 필요        |
| 지그재그   | 13~16 | 복잡, 좁은 구간 반복         |
| 3갈래 합류 | 17~20 | 세 방향 동시 방어           |

---

## 10. 난이도 곡선

| 스테이지   | HP 배율     | 속도 배율     | 특징               |
|:------:|:---------:|:---------:| ---------------- |
| 1      | **0.55×** | 0.85×     | 입문 — 학습 구간       |
| 2      | 0.65×     | 0.89×     | ↑                |
| 3      | 0.75×     | 0.93×     | ↑                |
| 4      | 0.85×     | 0.97×     | 정상 진입 직전         |
| **5**  | **1.00×** | **1.00×** | **기준선 (첫 중간보스)** |
| 10     | 1.65×     | 1.10×     | 첫 정식 보스          |
| 15     | 2.30×     | 1.20×     | 분열 보스            |
| **20** | **2.95×** | **1.30×** | **최종 보스**        |

```csharp
// 초반 학습 구간 (1~4): 부드러운 선형 증가
if (n <= 4) return 0.55 + (n - 1) * 0.10;
// 이후 (5~20): 0.13씩 상승 → 20스테이지에서 약 3배
return 1.00 + (n - 5) * 0.13;
```

> **설계 의도**: 1~4스테이지는 의도적으로 쉽게 — 타워 배치·업그레이드 흐름 학습.
> 5스테이지(첫 중간보스)부터 본격적 도전이 시작되는 경험 설계.

---

## 11. 영구 강화 시스템 (테크트리)

스테이지 클리어로 얻은 **★ 별**을 소비해 영구 업그레이드.

| 노드          | 효과                    | 최대 레벨 |
| ----------- | --------------------- | ----- |
| 타워 공격력 강화   | 전 타워 데미지 +%           | 3단계   |
| 타워 사거리 강화   | 전 타워 사거리 +%           | 3단계   |
| 타워 공격속도 강화  | 전 타워 쿨다운 감소           | 3단계   |
| 건설 비용 감소    | 타워 초기 비용 –%           | 2단계   |
| 적 체력 감소     | 전 적 HP 배율 감소          | 3단계   |
| 적 이동속도 감소   | 전 적 속도 배율 감소          | 2단계   |
| 처치 골드 보너스   | 적 처치마다 +골드            | 3단계   |
| 웨이브 클리어 보너스 | 웨이브 완료마다 +골드          | 2단계   |
| 화포 쿨다운 감소   | Meteor 재사용 시간 단축      | 2단계   |
| 지원군 지속 시간   | Reinforce 병사 체류 시간 +s | 2단계   |

- **메타 진행**: 어려운 스테이지 → 별 3개 도전 → 테크트리 강화 → 재도전 순환
- **다운그레이드**: 잘못 올린 노드는 환불 가능 (별 반환)

---

## 12. 스킬 시스템 & 조작 단축키

### 액티브 스킬 (2종)

| 단축키 | 스킬                 | 효과                     | 쿨타임     |
|:---:| ------------------ | ---------------------- |:-------:|
| `1` | 🔥 화포 (Meteor)     | 클릭 지점 반경 80, 폭발 250 피해 | **30초** |
| `2` | 🛡 지원군 (Reinforce) | 임시 병사 2명 소환, 15초간 전투   | **25초** |

> 테크트리로 Meteor 쿨타임 단축 / 지원군 지속 시간 연장 가능

### 게임 플로우 단축키

| 단축키     | 기능                            |
|:-------:| ----------------------------- |
| `N`     | 다음 웨이브 조기 호출 — 잔여시간 × 2 골드 보상 |
| `Space` | 일시정지 / 재개                     |
| `F`     | 2배속 토글 (패스트포워드)               |

### UI 인터랙션

- **타워 클릭** → 업그레이드·분기·판매 오버레이 팝업
- **타워 호버** → 사거리 인디케이터 원 표시
- **빈 타일 클릭** → 건설 메뉴 (허용된 타워 목록 + 비용)

---

## 13. 게임 내 UI / UX 디자인

### 부동 데미지 텍스트 (Floating Damage Labels)

```
물리 피해   → ⚪ 흰색
마법 피해   → 🟣 보라색
폭발 피해   → 🟠 주황색
크리티컬    → 🟡 노란색 + 굵게
```

- `DamageEvent` 큐: GameEngine이 이벤트 발생 → GamePage가 매 프레임 드레인 → 텍스트 스폰
- 텍스트는 위로 부상 후 페이드아웃 (애니메이션 0.8초)

### 기타 UI 요소

| 요소            | 위치      | 기능                               |
| ------------- | ------- | -------------------------------- |
| **보스 HP 카드**  | 좌측 상단   | 보스 이름 + 정확한 수치 (예: 2840/4000 HP) |
| **웨이브 배너**    | 화면 중앙   | 새 웨이브 시작 시 "Wave N 시작!" 알림       |
| **라이프 플래시**   | 화면 테두리  | 적이 본진 도달 시 빨간 테두리 깜빡             |
| **스테이지 인트로**  | 게임 시작 전 | 이번 스테이지에 출현할 적 종류 미리보기           |
| **웨이브 미리보기**  | HUD 하단  | 다음 웨이브 적 종류 텍스트 표시               |
| **사거리 인디케이터** | 타워 선택 시 | 반투명 원으로 사거리 범위 표시                |

---

## 14. 비주얼 시스템 — 100% 코드 생성

> 이미지 파일 0개 — 단일 EXE로 배포 가능한 이유

### EnemyFallbackImageFactory

```csharp
// DrawingContext로 적 스프라이트를 코드로 그림
// Freeze() → 크로스스레드 안전 + 캐시 재사용
static readonly Dictionary<EnemyKind, ImageSource> _cache = new();

public static ImageSource CreateSprite(EnemyKind kind)
{
    if (_cache.TryGetValue(kind, out var cached)) return cached;
    var dv = new DrawingVisual();
    using var dc = dv.RenderOpen();
    DrawEnemy(dc, kind);          // 종류별 맞춤 드로잉
    var img = new RenderTargetBitmap(64, 64, 96, 96, PixelFormats.Pbgra32);
    img.Render(dv);
    img.Freeze();                 // 불변화 → UI 스레드 없이 접근 가능
    return _cache[kind] = img;
}
```

### 종류별 드로잉 예시

| 적               | 표현 방법              |
| --------------- | ------------------ |
| 고블린 스카우트        | 삼각형 실루엣 + 속도선 3개   |
| 와이번             | 본체 타원 + 날개 폴리곤 2장  |
| 분열체 (SplitBody) | 좌우 타원 2개 + 갈라진 균열선 |
| 보스              | 왕관 모양 패스 + 진한 아우라  |

### 타워·투사체 비주얼

- **타워** — 이모지 + 분기별 색상 변화 라운드 사각형 (`TowerColorHex`)
- **투사체** — 타워별 고유 색상 (`ProjectileColorHex`) + 폭격은 포물선 궤적
- **이펙트** — 폭발·슬로우·도트 모두 반투명 색상 그라데이션 원

---

## 15. 렌더링 파이프라인

### 60fps 게임 루프

```csharp
// DispatcherTimer (16ms 간격 = ~60fps)
_timer = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(16) };
_timer.Tick += (_, _) =>
{
    double dt = _sw.Elapsed.TotalSeconds;
    _sw.Restart();
    _engine.Tick(dt);          // 게임 로직 업데이트
    Render();                   // Canvas 재구성
    DrainDamageEvents();        // 부동 데미지 텍스트 스폰
};
```

### Canvas 업데이트 전략

- **적** : `_enemyShapes` Dictionary (EnemyInstance → UIElement 쌍) 유지
  - 신규 적 → 캐시에 추가 / 사망·제거 → 캐시에서 삭제
  - 위치 = `Canvas.SetLeft/Top()` 매 프레임 갱신
- **타워** : 건설·판매 시에만 재생성 (매 프레임 재그리지 않음)
- **투사체** : Projectile.GetRenderPos() 결과 위치 갱신
- **이펙트** : TimeLeft 비율로 Opacity 계산 후 제거

### 2배속 구현

```csharp
double scale = Speed == GameSpeed.Fast ? 2.0 : 1.0;
dt *= scale;   // dt를 2배로 늘려 모든 로직이 2배 속도로 진행
```

---

## 16. SSOT 패턴 & 코드 품질

### SSOT (Single Source of Truth)

```csharp
// TowerCatalog.cs — 단 한 곳에서 타워 스탯 정의
public static readonly Dictionary<TowerKind, TowerDef> Towers = new()
{
    [TowerKind.Archer] = new TowerDef
    {
        Kind            = TowerKind.Archer,
        GuideColorHex   = "#8B4513",   // 도감 패널 배경색
        TowerColorHex   = "#CD853F",   // 게임 내 타워 색상
        ProjectileColorHex = "#3CB371", // 투사체 색상
        // ...스탯, 업그레이드, 분기 정보
    }
};
```

**변경 시 자동 반영되는 곳**: 게임 내 타워 시각화 / 업그레이드 오버레이 / 타워 도감 / 빌드 메뉴 비용 표시

### 독립된 3색 시스템

| 필드                   | 사용처            | 예시 (Archer)     |
| -------------------- | -------------- | --------------- |
| `GuideColorHex`      | 도감 패널 헤더 배경    | `#8B4513` (갈색)  |
| `TowerColorHex`      | 게임 내 타워 아이콘 배경 | `#CD853F` (연갈색) |
| `ProjectileColorHex` | 발사체 색상         | `#3CB371` (녹색)  |

### 기타 코드 품질

- **경고 0, 오류 0** 빌드
- **전역 예외 처리기** `App.xaml.cs` → `%TEMP%\krc_crash.log` 자동 기록
- **데드 코드 제거** — `BuildSlotsAround` 등 미사용 메서드 정리
- **static 초기화 순서 주의** — `StageNames[]` 를 `Stages = Build()` 보다 먼저 선언

---

## 17. 테스트

별도 `Tests` 프로젝트로 핵심 로직 검증

```
dotnet run --project Tests/KingdomRushClone.Tests/KingdomRushClone.Tests.csproj

✔ PASS  split boss creates 2 split mid bosses
✔ PASS  split mid boss creates 2 split bodies
✔ PASS  split body creates 3 split small enemies
✔ PASS  stage 13 uses split mid boss / stage 15 uses split boss

All 4 tests passed.  (0 failed, 0 skipped)
```

### 테스트 내용

| 테스트             | 검증 대상                                                |
| --------------- | ---------------------------------------------------- |
| SplitBoss 분열    | DeathSpawns에 SplitMidBoss × 2 정확히 포함                 |
| SplitMidBoss 분열 | DeathSpawns에 SplitBody × 2 정확히 포함                    |
| SplitBody 분열    | DeathSpawns에 SplitSmall × 3 정확히 포함                   |
| 스테이지 배치         | Stage 13 보스 웨이브 = SplitMidBoss, Stage 15 = SplitBoss |

> 분열 체인은 잘못 설정 시 무한 분열·게임 불능 상태로 이어질 수 있어 **테스트가 필수적**

---

## 18. 추가 화면 구성

| 화면          | 내용                                                 |
| ----------- | -------------------------------------------------- |
| **메인 메뉴**   | 총 획득 ★ 수, 클리어 진행도 (N/20 스테이지)                      |
| **스테이지 선택** | 5×4 그리드 · 잠금/해금 상태 · 획득 별 수 표시                     |
| **타워 도감**   | 5종 타워 스탯 전체 · 업그레이드 경로 · 분기 비교 · 배치 팁              |
| **테크트리**    | 10 노드 · 별 소비/환불 · 현재 레벨 시각화                        |
| **결과 화면**   | 별 1~3 애니메이션 · 잔여 라이프 · 남은 골드 · 재도전/스테이지 선택/테크트리 버튼 |
| **게임 화면**   | HUD + Canvas 렌더링 (상세 내용은 앞 슬라이드 참조)                |

### 타워 도감 SSOT 연동

```csharp
// TowerGuidePage.xaml.cs — TowerCatalog에서 직접 읽어 UI 생성
foreach (var kind in order)
{
    var def = TowerCatalog.Towers[kind];
    // def.Levels[i].Damage, def.BranchA.Name, def.GuideColorHex …
    // 카탈로그 수정 → 도감 자동 갱신
}
```

---

## 19. 향후 확장 계획

### 단기 (구현 준비됨)

- **사운드** — 효과음/BGM (WPF MediaElement 또는 NAudio)
- **이미지 에셋** — 프로그래매틱 도형 → 실제 스프라이트 교체 (팩토리 패턴 유지)
- **슬로우 타워 분기** — 현재 분기 없음 → BranchA/B 추가 여지

### 중기 (구조적 변경 필요)

- **새 적 종류** — 자기 보호막 적 (물리 면역 구간), 자살 폭탄 적 (폭발 피해)
- **스테이지 에디터** — StageCatalog 구조가 명확해 JSON 기반 외부 에디터 구현 가능
- **업적 시스템** — SaveManager 확장으로 추가 가능

### 장기 (플랫폼 변경)

- **모바일 포팅** — .NET MAUI (그래픽 레이어 교체) 또는 Unity 이식
- **멀티플레이** — 협동 모드: 각 플레이어가 다른 경로 담당
- **PvP 모드** — 상대 진영에 적 파견하는 공방 모드

---

## 20. 데모 & 정리

### 실행 방법

```bash
# 빌드
dotnet build KingdomRushClone.csproj

# 실행
dotnet run --project KingdomRushClone.csproj

# 테스트
dotnet run --project Tests/KingdomRushClone.Tests/KingdomRushClone.Tests.csproj
```

레포지토리: https://github.com/830348kcy-art/Tower_Defense

### 프로젝트 요약

| 항목     | 수치                 |
| ------ | ------------------ |
| 스테이지   | 20개 (5 테마 × 4스테이지) |
| 타워 종류  | 5종 (분기 포함 9개 폼)    |
| 적 종류   | 12종 (분열 체인 4종 포함)  |
| 웨이브    | 스테이지당 8~10 웨이브     |
| 테크 노드  | 10종 영구 강화          |
| 코드 라인  | 약 7,000 LoC        |
| 이미지 에셋 | **0개** (전부 코드 생성)  |
| 테스트    | 4개 단위 테스트, 전원 PASS |

---

## Q & A

> 감사합니다 🙏

**Team-Tower_Defense**  
WPF · .NET 8 · C# 12  
830346jky / 830348kcy-art
